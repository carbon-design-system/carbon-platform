name: Publish Micromanage
on:
  push:
    tags:
      - '@carbon-platform/micromanage-cli@*.*.*'

  # Enable running this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  publish:
    name: Publish
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎
        uses: actions/checkout@v2

      - name: Setup Node 🏗
        uses: actions/setup-node@v2
        with:
          node-version: '16'
          cache: 'npm'

      - name: Bootstrap Micromanage 🥾
        run: npm run micromanage:bootstrap

      - name: Publish 🚀
        run: node packages/micromanage publish @carbon-platform/micromanage-cli
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NODE_AUTH_TOKEN }}
